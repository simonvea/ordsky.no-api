{
  "name": "ordsky-api",
  "description": "The backend API for ordsky.no",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.437.0",
    "aws-xray-sdk": "^3.2.0"
  },
  "devDependencies": {
    "jest": "^24.7.1"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "jest": {
    "setupFiles": [
      "<rootDir>/jestEnv.js"
    ],
    "modulePathIgnorePatterns": [
      "<rootDir>/.aws-sam/"
    ]
  }
}
