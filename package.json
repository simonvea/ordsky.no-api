{
  "name": "ordsky-api",
  "description": "The backend API for ordsky.no",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.115",
    "@types/node": "^20.2.1",
    "typescript": "^5.0.4",
    "vitest": "^4.0.6"
  },
  "scripts": {
    "dev": "node --watch src/index.ts",
    "build": "tsc",
    "tsc": "tsc",
    "check": "tsc --noEmit",
    "install:all": "cd src && for D in */; do npm ci; done"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
